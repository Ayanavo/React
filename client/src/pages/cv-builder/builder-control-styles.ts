/** Shared toggle row styling for CV / cover letter builder option panels. */
export const BUILDER_TOGGLE_GROUP_CLASS = "flex w-32 shrink-0 gap-4";

/** Wider icon-only toggle rows (e.g. token interlink) — right-aligned with other w-32 controls. */
export const BUILDER_TOGGLE_GROUP_WIDE_CLASS = "ml-auto flex shrink-0 gap-4";
